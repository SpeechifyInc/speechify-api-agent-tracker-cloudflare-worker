{
  "name": "agent-tracker-cloudflare-worker",
  "version": "1.0.0",
  "description": "One-click deploy Cloudflare Worker for tracking AI agents on any site",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@usesapient/agent-tracker": "^0.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240501.0",
    "typescript": "^5.0.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.0.0"
  },
  "license": "MIT"
}
